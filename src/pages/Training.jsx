import {
    useAuthenticator,
    withAuthenticator,
    Flex,
    View,
    Tabs,
} from '@aws-amplify/ui-react'

const Training = () => {
    return <div>Training</div>
}
export default withAuthenticator(Training)
